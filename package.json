{
  "name": "apeirosyntharch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "test": "vitest",
    "typecheck": "tsc -p tsconfig.base.json --noEmit",
    "build": "npm run typecheck"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}

